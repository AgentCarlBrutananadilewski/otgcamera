package com.toyrobotworkshop.auspex.ui.theme

import androidx.compose.material3.Typography

/**
 * App typography — defaults to the M3 type scale (Roboto on most devices).
 * Update this file when adding custom fonts (e.g. Google Fonts Roboto).
 */
val AppTypography = Typography()
